interface:
    display_name: 'Remove Dead Code'
    short_description: 'Prove and safely remove unreachable or unused code'
    default_prompt: 'Use $dead-code-removal to identify and remove high-confidence dead code in this repository without changing behaviour or unrelated code.'

policy:
    allow_implicit_invocation: false
